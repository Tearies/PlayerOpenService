namespace WebApi
{
    public enum HttpMethods
    {
        OPTIONS = 0,
        GET,
        HEAD,
        POST,
        PUT,
        DELETE,
        CONNECT,
        TRACE,
        LOCK,
        UNLOCK,
        COPY,
        LINK,
        UNLINK,
        PURGE,
        PROPFIND,
        MKCOL,
        MOVE,
        VIEW
    }
}